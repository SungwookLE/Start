CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(TEST)

SET(SOURCES LIB_MAT.c main.c)

SET(HEADERS typedef.h LIB_MAT.h)


INCLUDE_DIRECTORIES(${CMAKE_CURRENT_BINARY_DIR})

ADD_EXECUTABLE(first.out ${SOURCES} ${HEADERS} )